{
  "name": "rust-actix-react-server",
  "version": "1.0.0",
  "description": "React SPA created by create-react-app rendered by a rust-actix web server",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "start-server": "cd actix-server && cargo watch -x run",
    "build-client": "cd react-client && yarn install && yarn build",
    "deploy": "cd actix-server && cargo build --release && cd ../ && npm run build-client"
  },
  "keywords": [
    "rust",
    "react",
    "static_files"
  ],
  "author": "clemb08",
  "license": "WTFPL"
}
